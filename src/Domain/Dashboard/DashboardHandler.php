<?php

namespace App\Domain\Dashboard;

/**
 * I generally avoid to call service classes "handler" but when
 * the module is small (contains less than 5 methods) I do it 
 * sometimes for simplicity. If however this class should one day
 * have more than a few functions, I would absolutely split it in
 * more specific service classes.
 */
class DashboardHandler
{

}